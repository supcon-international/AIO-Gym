# External agent (control the sim over MQTT) — core:
paho-mqtt>=2.0
numpy>=1.24

# Running a trained ONNX policy (mqtt_agent.py --policy onnx):
onnxruntime>=1.17

# Gymnasium env + PPO training + ONNX export (gym_env.py / train_ppo.py):
gymnasium>=0.29
stable-baselines3>=2.3
torch>=2.2
onnx>=1.16
